import { NotFound } from "../components"

export const NotFoundRoute = () => {
  return (
    <main className="flex min-h-full w-full justify-center py-24 text-center">
      <NotFound />
    </main>
  )
}
